<div class="center-page">
  <p class="eyebrow">Bientôt</p>
  <h1>{{ shop.name }}</h1>
  <p>La boutique ouvre très bientôt.</p>
  {%- form 'storefront_password', class: 'form-stack' -%}
    <input type="password" name="password" placeholder="Mot de passe">
    <button type="submit" class="btn">Entrer</button>
  {%- endform -%}
</div>
