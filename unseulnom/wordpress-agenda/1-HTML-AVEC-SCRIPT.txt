<section id="usn-agenda">

  <div class="usn-drapeau"><i class="b"></i><i class="w"></i><i class="r"></i></div>

  <nav class="usn-nav">
    <a class="usn-nav-brand" href="https://unseulnom.org/">
      <span class="usn-nav-fleche" aria-hidden="true">&#8592;</span> Un Seul Nom
    </a>
    <a class="usn-nav-don" href="https://www.helloasso.com/associations/eglise-protestante-tziganes/formulaires/3" target="_blank" rel="noopener noreferrer">Faire un don</a>
  </nav>

  <!-- ============================================================= -->
  <!-- EN-TETE                                                        -->
  <!-- ============================================================= -->
  <header class="usn-tete">
    <div class="usn-wrap">
      <div class="usn-tete-grille">
        <div class="usn-logo-mark usn-logo-noir usn-tete-logo" role="img" aria-label="Un Seul Nom"></div>
        <div>
          <span class="usn-eyebrow">L'agenda</span>
          <h1>&Agrave; venir</h1>
          <p class="usn-tete-sous">
            <b>&Eacute;vang&eacute;lisation &amp; Gu&eacute;rison</b>, une ville apr&egrave;s l'autre.
            Chaque rendez-vous est gratuit et ouvert &agrave; tous&nbsp;: croyants,
            curieux, malades, chercheurs de Dieu.
          </p>
        </div>
      </div>
    </div>
  </header>

  <!--
    &#9658; BANDEAU DEFILANT &#8212; la liste des villes, repetee deux fois pour que
      le defilement boucle sans saut. Si vous ajoutez une ville, ajoutez-la
      dans LES DEUX listes, sinon le ruban saccade.
  -->
  <div class="usn-defile" aria-hidden="true">
    <div class="usn-defile-piste">
      <div class="usn-defile-suite">
        <b>Avignon</b> <span>17.10.26</span> <i>&#10022;</i>
        <b>Madagascar</b> <span>14.11.26</span> <i>&#10022;</i>
        <b>Marseille</b> <span>06.03.27</span> <i>&#10022;</i>
        <b>Bordeaux</b> <span>15.05.27</span> <i>&#10022;</i>
      </div>
      <div class="usn-defile-suite">
        <b>Avignon</b> <span>17.10.26</span> <i>&#10022;</i>
        <b>Madagascar</b> <span>14.11.26</span> <i>&#10022;</i>
        <b>Marseille</b> <span>06.03.27</span> <i>&#10022;</i>
        <b>Bordeaux</b> <span>15.05.27</span> <i>&#10022;</i>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LA PROCHAINE DATE                                              -->
  <!-- ============================================================= -->
  <!--
    &#9658; COMPTE A REBOURS &#8212; data-target vise toujours la PROCHAINE date,
      au format 2026-10-17T16:00:00. A changer apres chaque evenement.
  -->
  <div class="usn-prochain" id="usn-countdown" data-target="2026-10-17T16:00:00">
    <div class="usn-wrap usn-prochain-grille">
      <div>
        <div class="usn-prochain-k">Prochaine date</div>
        <div class="usn-prochain-v">Avignon &middot; Samedi 17 octobre 2026</div>
      </div>
      <div class="usn-chrono">
        <div><b data-cd="days">0</b><small>Jours</small></div>
        <div><b data-cd="hours">00</b><small>Heures</small></div>
        <div><b data-cd="mins">00</b><small>Min</small></div>
        <div><b data-cd="secs">00</b><small>Sec</small></div>
      </div>
      <a class="usn-btn usn-btn-primary" href="https://unseulnom.org/evenement/">Je r&eacute;serve ma place</a>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- L'AGENDA                                                       -->
  <!-- ============================================================= -->
  <div class="usn-agenda" id="usn-dates">
    <div class="usn-wrap">

      <!--
        &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;
        &#9658; LES DATES &#8212; une ligne par evenement, groupees par annee.
          Les lignes arrivent par la gauche au scroll.

          POUR AJOUTER UNE DATE : copiez une ligne entiere
          <div class="usn-ligne"> et changez les textes. Pour une
          nouvelle annee, ajoutez un separateur <div class="usn-an">.

          Quand les inscriptions ouvrent, remplacez
            <span class="usn-pastille usn-attente">Bient&ocirc;t</span>
          par
            <span class="usn-pastille">Inscriptions ouvertes</span>
          et ajoutez le lien <a class="usn-reserver"> en dessous.
        &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;
      -->

      <div class="usn-an"><b>2026</b></div>

      <div class="usn-ligne">
        <div class="usn-jour"><b>17</b><span>Octobre</span></div>
        <div>
          <div class="usn-ville">Avignon</div>
          <div class="usn-lieu">Parc des expos &middot; 800 chemin des F&eacute;lons, Montfavet &middot; Portes 15h00, conf&eacute;rence 16h00</div>
          <div class="usn-invites">Pr&eacute;dication&nbsp;: Yannis Gautier, Chriss Campion &middot; Louange&nbsp;: Patcha&iuml; Reyes, Nikita Heugebaert, Ruben Debard</div>
        </div>
        <div class="usn-etat">
          <span class="usn-pastille">Inscriptions ouvertes</span>
          <a class="usn-reserver" href="https://unseulnom.org/evenement/">R&eacute;server <span aria-hidden="true">&#8594;</span></a>
        </div>
      </div>

      <div class="usn-ligne">
        <div class="usn-jour"><b>14</b><span>Novembre</span></div>
        <div>
          <div class="usn-ville">Madagascar</div>
          <div class="usn-lieu">Lieu et horaires &agrave; confirmer</div>
          <div class="usn-invites">Pr&eacute;dication&nbsp;: Pasteur Jack Brunet, Patane</div>
        </div>
        <div class="usn-etat">
          <span class="usn-pastille usn-attente">Bient&ocirc;t</span>
        </div>
      </div>

      <div class="usn-an"><b>2027</b></div>

      <div class="usn-ligne">
        <div class="usn-jour"><b>06</b><span>Mars</span></div>
        <div>
          <div class="usn-ville">Marseille</div>
          <div class="usn-lieu">Lieu et horaires &agrave; confirmer</div>
          <div class="usn-invites">Pr&eacute;dication&nbsp;: Yannis Gautier, Chriss Campion &middot; Louange&nbsp;: Patcha&iuml; Reyes, Nikita Heugebaert, Ruben Debard</div>
        </div>
        <div class="usn-etat">
          <span class="usn-pastille usn-attente">Bient&ocirc;t</span>
        </div>
      </div>

      <div class="usn-ligne">
        <div class="usn-jour"><b>15</b><span>Mai</span></div>
        <div>
          <div class="usn-ville">Bordeaux</div>
          <div class="usn-lieu">Lieu et horaires &agrave; confirmer</div>
          <div class="usn-invites">Pr&eacute;dication&nbsp;: Pasteur Marcello Tunasi &middot; Louange&nbsp;: Dan Luiten, Nikita Heugebaert</div>
        </div>
        <div class="usn-etat">
          <span class="usn-pastille usn-attente">Bient&ocirc;t</span>
        </div>
      </div>

    </div>
  </div>

  <!-- ============================================================= -->
  <!-- APPEL AUX EGLISES                                              -->
  <!-- ============================================================= -->
  <div class="usn-bande">
    <div class="usn-wrap">
      <h2>
        <span class="usn-mot">La prochaine</span>
        <span class="usn-mot">ville sera</span>
        <span class="usn-mot">peut-&ecirc;tre</span>
        <span class="usn-mot">la v&ocirc;tre.</span>
      </h2>
      <p>
        Vous &ecirc;tes pasteur ou responsable et vous souhaitez accueillir
        Un Seul Nom&nbsp;? &Eacute;crivez-nous&nbsp;: nous regardons ensemble
        les dates, la salle et la mobilisation des &eacute;glises locales.
      </p>
      <div class="usn-bande-actions">
        <a class="usn-btn usn-btn-clair" href="mailto:75unseulnomjesus@gmail.com?subject=Accueillir%20Un%20Seul%20Nom%20dans%20notre%20ville">Nous &eacute;crire</a>
        <a class="usn-btn usn-btn-ligne" href="https://www.helloasso.com/associations/eglise-protestante-tziganes/formulaires/3?utm_source=app_ha&amp;utm_campaign=share_campaign_button&amp;utm_medium=ios" target="_blank" rel="noopener noreferrer">Soutenir la mission</a>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- EDITIONS PASSEES                                               -->
  <!-- ============================================================= -->
  <div class="usn-passe">
    <div class="usn-wrap">
      <div class="usn-passe-tete">
        <div>
          <span class="usn-eyebrow">D&eacute;j&agrave; v&eacute;cu</span>
          <h2 style="margin-top:12px;">L&agrave; o&ugrave; nous sommes pass&eacute;s</h2>
        </div>
        <p>Les affiches reprennent leurs couleurs au passage. Faites glisser pour les parcourir.</p>
      </div>

      <!--
        &#9658; EDITIONS PASSEES &#8212; pour en ajouter une, copiez un bloc
          <figure class="usn-affiche"> et changez l'image et les textes.
      -->
      <div class="usn-pellicule">
        <figure class="usn-affiche">
          <div class="usn-affiche-img">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-1-768x948.jpg" alt="Affiche d'Un Seul Nom &agrave; Paris, 4 mai 2024" loading="lazy" onerror="this.remove()" />
          </div>
          <div class="usn-affiche-quand">4 mai 2024</div>
          <div class="usn-affiche-ou">Paris</div>
          <div class="usn-affiche-salle">Stade S&eacute;bastien Charl&eacute;ty</div>
        </figure>

        <figure class="usn-affiche">
          <div class="usn-affiche-img">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-3-768x954.jpg" alt="Affiche d'Un Seul Nom &agrave; Croissy-Beaubourg, 21 et 22 juin 2025" loading="lazy" onerror="this.remove()" />
          </div>
          <div class="usn-affiche-quand">21 &amp; 22 juin 2025</div>
          <div class="usn-affiche-ou">Croissy-Beaubourg</div>
          <div class="usn-affiche-salle">La Cit&eacute; Royale</div>
        </figure>

        <figure class="usn-affiche">
          <div class="usn-affiche-img">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-4-768x945.jpg" alt="Affiche d'Un Seul Nom &agrave; Wasquehal, 28 mars 2026" loading="lazy" onerror="this.remove()" />
          </div>
          <div class="usn-affiche-quand">28 mars 2026</div>
          <div class="usn-affiche-ou">Wasquehal</div>
          <div class="usn-affiche-salle">Espace Lille M&eacute;tropole</div>
        </figure>

        <figure class="usn-affiche">
          <div class="usn-affiche-img">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-5-768x950.jpg" alt="Affiche d'Un Seul Nom &agrave; Perpignan, 9 mai 2026" loading="lazy" onerror="this.remove()" />
          </div>
          <div class="usn-affiche-quand">9 mai 2026</div>
          <div class="usn-affiche-ou">Perpignan</div>
          <div class="usn-affiche-salle">Palais des congr&egrave;s</div>
        </figure>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LETTRE D'INFORMATION                                           -->
  <!-- ============================================================= -->
  <div class="usn-lettre" id="usn-alerte">
    <div class="usn-wrap usn-lettre-grille">
      <div>
        <span class="usn-eyebrow">Ne ratez aucune date</span>
        <h2 style="margin-top:12px;">Soyez pr&eacute;venu</h2>
        <p>
          D&egrave;s qu'une ville est annonc&eacute;e, vous recevez un message&nbsp;:
          avant l'ouverture des inscriptions, et avant que la salle ne se remplisse.
        </p>
      </div>

      <div>
        <!-- Le formulaire part dans l'iframe cache : le visiteur reste sur
             la page au lieu d'etre redirige vers Brevo. -->
        <iframe name="usn-brevo" title="Inscription" style="display:none;border:0;width:0;height:0"></iframe>

        <form class="usn-form" id="usn-agenda-form" method="POST"
              action="https://f8944495.sibforms.com/serve/MUIFAIs9H4MOwtfawQ-nUiiNQjRmcSNQ51wCfEkSkK4X91IeG5GyZDWqGDV3oD_sUQHzLsQUO6Ky6EqX9G_I6VWvy8H5St2aXpxdhmXp81Jkl34fooTe9ukSalttOBmz1T6EwxTHhs9WLKWs2tBPCT1fhe_uubwAitTQjHmRHfmqBxQBcLifIZRDcFP9W3Vvzs_hPEa9_OySJBsT"
              target="usn-brevo" novalidate>
          <div class="usn-form-duo">
            <input id="usn-nom" name="NOM" type="text" placeholder="Votre nom" aria-label="Nom" required />
            <input id="usn-email" name="EMAIL" type="email" placeholder="vous@email.com" aria-label="Adresse e-mail" required />
          </div>

          <!-- Piege anti-robot de Brevo : ce champ doit rester vide. -->
          <input type="text" name="email_address_check" value="" tabindex="-1"
                 aria-hidden="true" autocomplete="off"
                 style="position:absolute;left:-9999px;width:1px;height:1px;opacity:0" />
          <input type="hidden" name="locale" value="fr" />

          <button type="submit" class="usn-btn usn-btn-primary">Pr&eacute;venez-moi</button>
          <p class="usn-consent">
            En vous inscrivant, vous acceptez de recevoir des informations de la
            part d'Un Seul Nom. D&eacute;sinscription possible &agrave; tout moment.
          </p>
        </form>

        <div class="usn-msg" id="usn-form-msg" role="status" aria-live="polite"></div>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- PIED DE PAGE                                                   -->
  <!-- ============================================================= -->
  <footer class="usn-footer">
    <div class="usn-wrap">
      <div class="usn-logo-mark usn-logo-blanc usn-footer-mark" role="img" aria-label="Un Seul Nom"></div>
      <div class="usn-footer-logo">Un Seul Nom</div>
      <p style="margin-top:10px;">&copy; 2026 Un seul Nom. Tous droits r&eacute;serv&eacute;s.</p>
      <p style="margin-top:14px;">
        <a href="https://unseulnom.org/">Retour au site</a> &nbsp;&middot;&nbsp;
        <a href="https://unseulnom.org/evenement/">Avignon</a>
      </p>
    </div>
  </footer>

  <div class="usn-drapeau"><i class="b"></i><i class="w"></i><i class="r"></i></div>

</section>

<script>
/* Tout le code attend que la page soit construite. Sans cela, si le script
   est insere avant le contenu (ce que font certains plugins WordPress), il
   ne trouve aucun element et s'arrete sans rien faire. */
(function () {
  function usnInit() {

  /* ---- Compte a rebours ---- */
  (function () {
    var box = document.getElementById('usn-countdown');
    if (!box) return;
    var target = new Date(box.getAttribute('data-target') || '').getTime();
    if (isNaN(target)) return;
    var els = {
      days: box.querySelector('[data-cd="days"]'),
      hours: box.querySelector('[data-cd="hours"]'),
      mins: box.querySelector('[data-cd="mins"]'),
      secs: box.querySelector('[data-cd="secs"]')
    };
    function pad(n) { return (n < 10 ? '0' : '') + n; }
    function tick() {
      var diff = Math.max(0, target - Date.now());
      var s = Math.floor(diff / 1000);
      var d = Math.floor(s / 86400); s -= d * 86400;
      var h = Math.floor(s / 3600); s -= h * 3600;
      var m = Math.floor(s / 60); s -= m * 60;
      if (els.days) els.days.textContent = d;
      if (els.hours) els.hours.textContent = pad(h);
      if (els.mins) els.mins.textContent = pad(m);
      if (els.secs) els.secs.textContent = pad(s);
    }
    tick();
    setInterval(tick, 1000);
  })();


  /* =================================================================
     MOTEUR D'ANIMATION AU SCROLL
     Reproduit le comportement de GSAP ScrollTrigger en mode " scrub " :
     - les elements partent d'une demi-hauteur d'ecran plus bas (50svh),
     - ils remontent AU RYTHME DU SCROLL (et redescendent si on remonte),
     - avec un chevauchement entre les elements d'un meme groupe,
     - le tout lisse (inertie) pour un rendu fluide.
     Sans JS ou avec " prefers-reduced-motion ", tout reste visible.
     ================================================================= */
  (function () {
    var root = document.getElementById('usn-agenda');
    if (!root) return;
    var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    function clamp01(v) { return v < 0 ? 0 : v > 1 ? 1 : v; }
    function easeOut(t) { return 1 - Math.pow(1 - t, 3); }
    function vh() { return window.innerHeight || 800; }

    /* ---------- 1. Decoupe la punchline en mots ---------- */
    var punch = document.getElementById('usn-punch');
    var words = [];
    if (punch) {
      var lines = punch.querySelectorAll('[data-usn-reveal]');
      for (var l = 0; l < lines.length; l++) {
        var parts = (lines[l].textContent || '').trim().split(/\s+/);
        lines[l].textContent = '';
        for (var k = 0; k < parts.length; k++) {
          var sp = document.createElement('span');
          sp.className = 'usn-word';
          sp.textContent = parts[k];
          lines[l].appendChild(sp);
          if (k < parts.length - 1) lines[l].appendChild(document.createTextNode(' '));
          words.push(sp);
        }
      }
      for (var w = 0; w < words.length; w++) {
        var a = Math.sin(w * 12.9898) * 43758.5453; a -= Math.floor(a);
        var b = Math.sin(w * 78.233) * 12543.123;  b -= Math.floor(b);
        words[w].__dx = (a - 0.5) * 2 * Math.min(window.innerWidth * 0.45, 520);
        words[w].__dy = (b - 0.5) * 2 * 260;
        words[w].__rot = (a - 0.5) * 55;
      }
    }

    if (reduce) return; // mouvement desactive : tout reste en place

    /* ---------- 2. Declare les groupes animes ---------- */
    // rise : monte depuis 50svh ; slide : arrive de la gauche ; pop : grossit
    var groups = [];
    function group(sel, opts) {
      var els = root.querySelectorAll(sel);
      if (els.length) groups.push({ els: els, o: opts });
    }
    group('.usn-an',      { type: 'rise',  dist: 0.22, stagger: 0,    span: 0.45 });
    group('.usn-ligne',   { type: 'slide', dist: 0.34, stagger: 0.13, span: 0.58 });
    group('.usn-affiche', { type: 'rise',  dist: 0.40, stagger: 0.11, span: 0.58 });
    group('.usn-mot',     { type: 'rise',  dist: 0.26, stagger: 0.14, span: 0.50 });

    var all = [];
    for (var g = 0; g < groups.length; g++) {
      for (var i = 0; i < groups[g].els.length; i++) {
        var el = groups[g].els[i];
        all.push({
          el: el, i: i, o: groups[g].o, p: 0, top: 0,
          rot: parseFloat(el.getAttribute('data-rot') || '0')
        });
      }
    }

    function scrollY() { return window.pageYOffset || document.documentElement.scrollTop || 0; }

    /* Position NATURELLE de chaque element (transform neutralise), mise en
       cache : sinon le decalage applique fausse la mesure et l'element
       n'entre jamais dans la zone de declenchement. */
    function measureAll() {
      for (var i = 0; i < all.length; i++) {
        var it = all[i], prev = it.el.style.transform;
        it.el.style.transform = 'none';
        it.top = it.el.getBoundingClientRect().top + scrollY();
        it.el.style.transform = prev;
      }
    }
    measureAll();

    /* ---------- Portraits : couleur au passage au centre ----------
       Chaque photo se colore a mesure qu'elle approche du centre de
       l'ecran, puis retourne au noir et blanc en s'en eloignant. */
    var photos = [];
    (function () {
      var imgs = root.querySelectorAll('.usn-affiche img');
      for (var i = 0; i < imgs.length; i++) photos.push({ el: imgs[i], p: 0 });
    })();

    function cibleCouleur(ph) {
      var r = ph.el.getBoundingClientRect();
      if (!r.height) return 0;
      var h = vh();
      var ecart = Math.abs((r.top + r.height / 2) - h / 2);
      // Pleine couleur au centre, noir et blanc au-dela de 42% de l'ecran.
      return clamp01(1 - ecart / (h * 0.42));
    }

    function peindre(ph) {
      var e = easeOut(ph.p);
      ph.el.style.filter = 'grayscale(' + (1 - e).toFixed(3) + ') contrast(' + (1 + 0.06 * (1 - e)).toFixed(3) + ')';
      ph.el.style.transform = 'scale(' + (1 + 0.035 * e).toFixed(4) + ')';
    }
    for (var ip = 0; ip < photos.length; ip++) peindre(photos[ip]);

    // etat initial immediat (evite tout " flash " de contenu deja en place)
    for (var s = 0; s < all.length; s++) apply(all[s], 0);

    /* ---------- 3. Calcul de l'avancement lie au scroll ---------- */
    function target(item) {
      var h = vh();
      var top = item.top - scrollY();
      // 0 quand le haut de l'element entre par le bas, 1 quand il a monte
      var raw = (h * 0.92 - top) / (h * item.o.span);
      var st = item.i * item.o.stagger;
      return clamp01((clamp01(raw) - st) / Math.max(0.2, 1 - st));
    }

    function apply(item, p) {
      var e = easeOut(p), inv = 1 - e, o = item.o, st = item.el.style;
      var rot = item.rot ? ' rotate(' + item.rot + 'deg)' : '';
      if (o.type === 'rise') {
        st.transform = 'translate3d(0,' + (inv * o.dist * vh()).toFixed(1) + 'px,0)' + rot;
      } else if (o.type === 'slide') {
        st.transform = 'translate3d(' + (-inv * o.dist * Math.min(window.innerWidth, 900)).toFixed(1) + 'px,0,0)' + rot;
      } else if (o.type === 'slideR') {
        st.transform = 'translate3d(' + (inv * o.dist * Math.min(window.innerWidth, 900)).toFixed(1) + 'px,0,0)' + rot;
      } else { // pop
        st.transform = 'scale(' + (0.3 + 0.7 * e).toFixed(3) + ')' + rot;
      }
      st.opacity = e.toFixed(3);
    }

    /* ---------- 4. Punchline : les mots se rassemblent ---------- */
    var punchP = 0;
    // Ancre sur la premiere ligne de texte (et non sur la section, bien plus
    // haute) : les mots se rassemblent des que le texte entre a l'ecran.
    var punchAnchor = punch && punch.querySelector('[data-usn-reveal]');
    function punchTarget() {
      if (!punchAnchor) return 0;
      var r = punchAnchor.getBoundingClientRect(), h = vh();
      return clamp01((h * 1.0 - r.top) / (h * 0.5));
    }
    function punchApply(p) {
      var n = words.length;
      for (var i = 0; i < n; i++) {
        var t = clamp01((p - (i / n) * 0.45) / 0.55);
        var e = easeOut(t), inv = 1 - e, ws = words[i];
        ws.style.transform = 'translate3d(' + (ws.__dx * inv).toFixed(1) + 'px,' + (ws.__dy * inv).toFixed(1) + 'px,0) rotate(' + (ws.__rot * inv).toFixed(1) + 'deg)';
        ws.style.opacity = e.toFixed(3);
        ws.style.filter = inv > 0.02 ? 'blur(' + (inv * 9).toFixed(1) + 'px)' : 'none';
      }
      var sub = punch && punch.querySelector('[data-usn-sub]');
      if (sub) {
        var sp = clamp01((p - 0.6) / 0.4);
        sub.style.opacity = sp.toFixed(3);
        sub.style.transform = 'translate3d(0,' + ((1 - sp) * 40).toFixed(1) + 'px,0)';
      }
    }
    punchApply(0);

    /* ---------- 5. Parallaxe du heros ---------- */
    var hero = root.querySelector('.usn-hero');
    var heroInner = root.querySelector('.usn-hero-inner');
    var heroVideo = root.querySelector('.usn-hero-video');

    /* ---------- 6. Boucle de rendu (lissage type ScrollSmoother) ---------- */
    var running = false;
    function frame() {
      var moving = false;

      for (var i = 0; i < all.length; i++) {
        var it = all[i], t = target(it);
        it.p += (t - it.p) * 0.12;             // lissage
        if (Math.abs(t - it.p) > 0.001) moving = true; else it.p = t;
        apply(it, it.p);
      }

      if (punch) {
        var tp = punchTarget();
        punchP += (tp - punchP) * 0.12;
        if (Math.abs(tp - punchP) > 0.001) moving = true; else punchP = tp;
        punchApply(punchP);
      }

      for (var k = 0; k < photos.length; k++) {
        var ph = photos[k], tc = cibleCouleur(ph);
        ph.p += (tc - ph.p) * 0.14;
        if (Math.abs(tc - ph.p) > 0.004) moving = true; else ph.p = tc;
        peindre(ph);
      }

      if (hero && heroInner) {
        var hr = hero.getBoundingClientRect();
        var prog = clamp01(-hr.top / Math.max(1, hr.height));
        heroInner.style.transform = 'translate3d(0,' + (prog * 130).toFixed(1) + 'px,0)';
        heroInner.style.opacity = (1 - prog * 0.85).toFixed(3);
        if (heroVideo) heroVideo.style.transform = 'scale(' + (1 + prog * 0.16).toFixed(3) + ')';
      }

      if (moving) { requestAnimationFrame(frame); } else { running = false; }
    }
    function kick() { if (!running) { running = true; requestAnimationFrame(frame); } }

    window.addEventListener('scroll', kick, { passive: true });
    window.addEventListener('resize', function () { measureAll(); kick(); });
    window.addEventListener('load', function () { measureAll(); kick(); });
    kick();
  })();


  /* ---- Lettre d'information ----
     Pas de compteur de places ici : la page annonce des dates, elle ne
     reserve rien. Le formulaire part vers Brevo dans un iframe cache. */
  (function () {
    var form = document.getElementById('usn-agenda-form');
    if (!form) return;
    var msg = document.getElementById('usn-form-msg');

    form.addEventListener('submit', function (e) {
      var nom   = form.querySelector('#usn-nom');
      var email = form.querySelector('#usn-email');
      if (!nom.value.trim() || !/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email.value)) {
        e.preventDefault();
        (nom.value.trim() ? email : nom).focus();
        return;
      }
      if (msg) {
        msg.textContent = 'Merci ! Vous serez pr\u00e9venu d\u00e8s qu\'une nouvelle date est annonc\u00e9e.';
        msg.style.display = 'block';
      }
      setTimeout(function () { form.reset(); }, 100);
    });
  })();

  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', usnInit);
  } else {
    usnInit();
  }
})();
</script>
