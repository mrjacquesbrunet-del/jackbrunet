<section id="usn-accueil">

  <div class="usn-grain" aria-hidden="true"></div>

  <!-- ============================================================= -->
  <!-- BARRE DE NAVIGATION                                            -->
  <!-- ============================================================= -->
  <nav class="usn-nav" id="usn-nav">
    <a class="usn-nav-nom usn-logo-mot usn-mot-noir" href="#usn-haut" aria-label="Un Seul Nom"></a>
    <div class="usn-nav-actions">
      <a class="usn-nav-soutien" href="https://www.helloasso.com/associations/eglise-protestante-tziganes/formulaires/3" target="_blank" rel="noopener noreferrer">Soutien</a>
      <button type="button" class="usn-nav-menu" id="usn-bouton-menu" aria-expanded="false" aria-controls="usn-menu" aria-label="Ouvrir le menu">
        <i aria-hidden="true"></i><i aria-hidden="true"></i>
      </button>
    </div>
  </nav>

  <!-- Le panneau ouvert par le bouton a deux traits. -->
  <div class="usn-menu" id="usn-menu" hidden>
    <div class="usn-menu-liens">
      <a href="https://unseulnom.org/evenement/">Prochain &eacute;v&eacute;nement</a>
      <a href="#usn-vision">La vision</a>
      <a href="#usn-galerie">Les villes</a>
      <a href="#usn-eglises">&Eacute;glises</a>
      <a href="https://www.helloasso.com/associations/eglise-protestante-tziganes/formulaires/3" target="_blank" rel="noopener noreferrer">Soutenir la mission</a>
    </div>
    <div class="usn-menu-pied">
      <span>Un Seul Nom &mdash; &Eacute;vang&eacute;lisation &amp; gu&eacute;rison</span>
      <a href="mailto:75unseulnomjesus@gmail.com">75unseulnomjesus@gmail.com</a>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LA SC&#200;NE D'OUVERTURE                                           -->
  <!-- Le logo s'efface au scroll et laisse passer l'image derri&#232;re.  -->
  <!-- ============================================================= -->
  <header class="usn-scene" id="usn-haut">
    <div class="usn-scene-fixe">

      <!-- couche du dessous : l'image, r&#233;v&#233;l&#233;e en descendant -->
      <div class="usn-couche usn-couche-image">
        <!--
          &#9658; VID&#201;O DE FOND &#8212; le teaser du prochain &#233;v&#233;nement.
          Pour la changer, remplacez l'adresse dans la ligne <source>.
        -->
        <video class="usn-hero-video" autoplay muted loop playsinline poster="">
          <source src="https://unseulnom.org/wp-content/uploads/2026/08/teaser-web.mp4" type="video/mp4" />
          <source src="https://unseulnom.org/wp-content/uploads/2026/08/Teaser-1.mp4" type="video/mp4" />
        </video>
        <div class="usn-voile"></div>
        <div>
          <h1>Un Seul Nom</h1>
          <p class="usn-sous">&Eacute;vang&eacute;lisation &amp; gu&eacute;rison, de ville en ville. Depuis 2024.</p>
        </div>

        <!-- Le bouton du son appartient a l'image : il doit disparaitre
             avec elle, et non flotter sur la creme du premier bloc. -->
        <button type="button" class="usn-son" id="usn-son" aria-pressed="false">
          <span class="usn-son-icone" aria-hidden="true">&#9834;</span>
          <span class="usn-son-texte">Activer le son</span>
        </button>
      </div>

      <!-- couche du dessus : le fond creme, le logo, et les photos que
           le curseur decouvre en passant dessus -->
      <div class="usn-couche usn-couche-logo" id="usn-bloc1">

        <div class="usn-texture" aria-hidden="true">
          <i class="usn-lavis usn-l1"></i>
          <i class="usn-lavis usn-l2"></i>
        </div>
        <div class="usn-matiere" aria-hidden="true"></div>
        <div class="usn-filets" aria-hidden="true"></div>

        <div class="usn-marque">
          <div class="usn-logo-badge" role="img" aria-label="Un Seul Nom"></div>

          <!-- La couronne : le texte suit un cercle et tourne autour de la
               pastille. textLength force la phrase a faire exactement le
               tour, quelle que soit la police du visiteur. -->
          <svg class="usn-couronne" viewBox="0 0 220 220" aria-hidden="true">
            <defs>
              <path id="usn-cercle-texte" fill="none"
                    d="M110,110 m-96,0 a96,96 0 1,1 192,0 a96,96 0 1,1 -192,0" />
            </defs>
            <text>
              <!-- &#9658; Le nom est deja ecrit dans la pastille : la couronne
                   dit ce que le nom ne dit pas. Changez la phrase ici. -->
              <textPath href="#usn-cercle-texte" startOffset="0" textLength="603" lengthAdjust="spacing">Des &eacute;glises unies &#8226; &Eacute;vang&eacute;lisation &amp; gu&eacute;rison &#8226; De ville en ville &#8226; Entr&eacute;e libre &#8226;</textPath>
            </text>
          </svg>
        </div>

        <!-- le filet du bord droit -->
        <div class="usn-rail" aria-hidden="true">
          <i></i><span>Depuis 2024 &#8212; France</span><i></i>
        </div>

        <!--
          &#9658; LES PHOTOS D&#201;COUVERTES PAR LES BALAYAGES.
            Quatre lames se relaient : &#224; droite, &#224; gauche, en haut, en bas.
            Dans chacune, mettez autant de photos que vous voulez &#8212; &#224;
            chaque passage, c'est la suivante qui appara&#238;t.
        -->
        <div class="usn-balai usn-b1" aria-hidden="true">
          <img class="est-vue" src="https://unseulnom.org/wp-content/uploads/2026/07/programme1.webp" alt="" />
          <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme3.webp" alt="" />
        </div>
        <div class="usn-balai usn-b2" aria-hidden="true">
          <img class="est-vue" src="https://unseulnom.org/wp-content/uploads/2026/07/programme2.webp" alt="" />
          <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme4.webp" alt="" />
        </div>
        <div class="usn-balai usn-b3" aria-hidden="true">
          <img class="est-vue" src="https://unseulnom.org/wp-content/uploads/2026/07/programme3.webp" alt="" />
          <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme1.webp" alt="" />
        </div>
        <div class="usn-balai usn-b4" aria-hidden="true">
          <img class="est-vue" src="https://unseulnom.org/wp-content/uploads/2026/07/programme4.webp" alt="" />
          <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme2.webp" alt="" />
        </div>

        <!-- la fen&#234;tre qui suit le doigt ou la souris -->
        <div class="usn-hublot" id="usn-hublot" aria-hidden="true">
          <img class="est-vue" src="https://unseulnom.org/wp-content/uploads/2026/07/programme1.webp" alt="" />
          <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme2.webp" alt="" />
          <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme3.webp" alt="" />
          <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme4.webp" alt="" />
        </div>
        <div class="usn-anneau" id="usn-anneau" aria-hidden="true"></div>

        <!-- le prochain rendez-vous, cliquable -->
        <a class="usn-prochain" href="https://unseulnom.org/evenement/">
          <span class="usn-prochain-titre">Prochain &eacute;v&eacute;nement</span>
          <span class="usn-prochain-corps">
            <svg class="usn-plan" viewBox="0 0 100 100" aria-hidden="true">
              <g><path d="M2.0 62.4L44.5 70.8L60.1 71.8L76.5 79.7L85.5 88.2L88.3 88.9L95.3 88.6L96.0 86.8L91.5 81.1L95.7 80.0L98.0 76.0L83.1 69.1L82.6 66.1L83.9 58.2L81.8 53.7L79.4 52.6L79.4 47.4L76.0 42.3L69.5 42.1L61.6 43.9L52.4 42.6L53.0 37.6L56.8 36.6L60.3 31.0L60.5 19.1L56.2 11.1L55.5 16.6L53.0 19.4L41.2 16.7L39.0 18.0L38.1 24.8L40.4 30.3L40.7 34.0L38.1 36.9L29.7 41.1L25.8 48.5L21.8 53.2L17.0 55.8L13.3 55.1L8.0 56.8L2.0 62.4Z" /></g>
            </svg>
            <span>
              <span class="usn-prochain-ville">Avignon</span>
              <span class="usn-prochain-date">Samedi 17 octobre 2026 &middot; Parc des expos</span>
            </span>
          </span>
          <span class="usn-prochain-bouton">R&eacute;serve ta place <i aria-hidden="true">&#8594;</i></span>
        </a>

        <div class="usn-scene-invite">Faites d&eacute;filer <span aria-hidden="true">&#8595;</span></div>
      </div>
    </div>
  </header>

  <!--
    &#9658; BANDEAU D&#201;FILANT &#8212; la liste est r&#233;p&#233;t&#233;e deux fois pour que la boucle
      se referme sans saut. Modifiez les DEUX listes, sinon le ruban saccade.
  -->
  <div class="usn-defile usn-defile-bleu" aria-hidden="true">
    <div class="usn-defile-piste">
      <div class="usn-defile-suite">
        <b>&Eacute;vang&eacute;lisation</b> <i>&#10022;</i> <b>Gu&eacute;rison</b> <i>&#10022;</i>
        <b>Unit&eacute;</b> <i>&#10022;</i> <b>Entr&eacute;e libre</b> <i>&#10022;</i>
      </div>
      <div class="usn-defile-suite">
        <b>&Eacute;vang&eacute;lisation</b> <i>&#10022;</i> <b>Gu&eacute;rison</b> <i>&#10022;</i>
        <b>Unit&eacute;</b> <i>&#10022;</i> <b>Entr&eacute;e libre</b> <i>&#10022;</i>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LA VISION, EN SIX LIGNES                                       -->
  <!-- ============================================================= -->
  <div class="usn-vision" id="usn-vision">
    <div class="usn-wrap">
      <span class="usn-cle" data-usn-monte>La vision</span>
      <div class="usn-vision-lignes">
        <p data-usn-monte>Nous rassemblons des <em>&eacute;glises</em> et des minist&egrave;res diff&eacute;rents.</p>
        <p data-usn-monte>Nous allons de ville en ville annoncer <strong>J&eacute;sus-Christ</strong>.</p>
        <p data-usn-monte>Dans des salles ouvertes &agrave; tous, gratuitement.</p>
        <p data-usn-monte>Nous croyons que l'<em>unit&eacute;</em> du corps de Christ ouvre les c&oelig;urs,</p>
        <p data-usn-monte>que la <strong>gu&eacute;rison</strong> suit l'annonce de l'&Eacute;vangile,</p>
        <p data-usn-monte>et qu'une ville confi&eacute;e &agrave; Dieu peut changer.</p>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LA GALERIE                                                     -->
  <!-- ============================================================= -->
  <div class="usn-galerie" id="usn-galerie">
    <div class="usn-wrap">
      <span class="usn-cle" data-usn-monte>D&eacute;j&agrave; v&eacute;cu</span>

      <!--
        &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;
        &#9658; LA GALERIE &#8212; pour ajouter une photo, copiez un bloc <figure>
          entier et changez l'image et la l&#233;gende. La mosa&#239;que se
          r&#233;organise toute seule : vous pouvez en mettre des dizaines.

          Les photos s'affichent en noir et blanc et reprennent leurs
          couleurs en passant au centre de l'&#233;cran.
        &#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;&#9552;
      -->
      <div class="usn-mosaique">
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-1-768x948.jpg" alt="Un Seul Nom, Paris 2024" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Paris</b>, 2024 &middot; Stade S&eacute;bastien Charl&eacute;ty</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme2.webp" alt="Un Seul Nom, Paris 2024" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Paris</b>, 2024 &middot; La foule</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-3-768x954.jpg" alt="Un Seul Nom, Croissy-Beaubourg 2025" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Croissy-Beaubourg</b>, 2025 &middot; La Cit&eacute; Royale</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme3.webp" alt="Un Seul Nom, Croissy-Beaubourg 2025" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Croissy-Beaubourg</b>, 2025 &middot; Sur sc&egrave;ne</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-4-768x945.jpg" alt="Un Seul Nom, Wasquehal 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Wasquehal</b>, 2026 &middot; Espace Lille M&eacute;tropole</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme1.webp" alt="Un Seul Nom, Wasquehal 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Wasquehal</b>, 2026 &middot; Avant l'ouverture</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-5-768x950.jpg" alt="Un Seul Nom, Perpignan 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Perpignan</b>, 2026 &middot; Palais des congr&egrave;s</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/programme4.webp" alt="Un Seul Nom, Perpignan 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Perpignan</b>, 2026 &middot; La pr&eacute;dication</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/chriss.webp" alt="Un Seul Nom, Chriss Campion 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Chriss Campion</b>, 2026 &middot; Pr&eacute;dication</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/yannis.webp" alt="Un Seul Nom, Yannis Gautier 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Yannis Gautier</b>, 2026 &middot; Pr&eacute;dication</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/patchai.webp" alt="Un Seul Nom, Patcha&iuml; Reyes 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Patcha&iuml; Reyes</b>, 2026 &middot; Louange</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/nikita.webp" alt="Un Seul Nom, Nikita Heugebaert 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Nikita Heugebaert</b>, 2026 &middot; Louange</figcaption>
        </figure>
        <figure class="usn-vue">
          <div class="usn-vue-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/ruben.webp" alt="Un Seul Nom, Ruben Debard 2026" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Ruben Debard</b>, 2026 &middot; Louange</figcaption>
        </figure>
      </div>

      <!-- &#9658; Mettez ces chiffres &#224; jour apr&#232;s chaque &#233;dition. -->
      <div class="usn-chiffres">
        <div class="usn-chiffre"><b>5</b><span>villes depuis 2024</span></div>
        <div class="usn-chiffre"><b>4</b><span>dates annonc&eacute;es</span></div>
        <div class="usn-chiffre"><b>0&nbsp;&euro;</b><span>entr&eacute;e libre, toujours</span></div>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LES DEUX VISAGES QUI SE RAPPROCHENT                            -->
  <!-- ============================================================= -->
  <div class="usn-rencontre" id="usn-rencontre">
    <div class="usn-wrap">
      <span class="usn-cle">Le prochain rendez-vous</span>
      <h2>Avignon &middot; samedi 17 octobre 2026</h2>

      <!--
        &#9658; LES DEUX VISAGES &#8212; ils arrivent chacun d'un bord et se
          rejoignent au centre. Remplacez les deux images et les deux
          l&#233;gendes par les portraits de votre choix.
      -->
      <div class="usn-visages">
        <figure class="usn-visage" data-usn-visage="gauche">
          <div class="usn-visage-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/yannis.webp" alt="Yannis Gautier" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Yannis Gautier</b>Pr&eacute;dication</figcaption>
        </figure>

        <div class="usn-entre">
          <a class="usn-btn usn-btn-rouge" href="https://unseulnom.org/evenement/">Le prochain &eacute;v&eacute;nement</a>
          <a class="usn-btn usn-btn-bleu" href="https://www.helloasso.com/associations/eglise-protestante-tziganes/formulaires/3" target="_blank" rel="noopener noreferrer">Soutiens la mission</a>
        </div>

        <figure class="usn-visage" data-usn-visage="droite">
          <div class="usn-visage-cadre">
            <img src="https://unseulnom.org/wp-content/uploads/2026/07/patchai.webp" alt="Patcha&iuml; Reyes" loading="lazy" onerror="this.parentNode.parentNode.remove()" />
          </div>
          <figcaption><b>Patcha&iuml; Reyes</b>Louange</figcaption>
        </figure>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LES AFFICHES QUI MONTENT                                       -->
  <!-- ============================================================= -->
  <div class="usn-affiches">
    <div class="usn-wrap">
      <span class="usn-cle" data-usn-monte>Les affiches</span>

      <!--
        &#9658; Chaque colonne monte &#224; sa propre vitesse : le nombre apr&#232;s
          data-usn-parallaxe r&#232;gle cette vitesse (1 = normale).
      -->
      <div class="usn-colonnes">
        <div class="usn-colonne" data-usn-parallaxe="1.5">
          <figure class="usn-affiche">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-1-768x948.jpg" alt="Affiche de Paris 2024" loading="lazy" onerror="this.parentNode.remove()" />
            <figcaption>Paris &middot; 2024</figcaption>
          </figure>
        </div>
        <div class="usn-colonne" data-usn-parallaxe="0.5">
          <figure class="usn-affiche">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-3-768x954.jpg" alt="Affiche de Croissy-Beaubourg 2025" loading="lazy" onerror="this.parentNode.remove()" />
            <figcaption>Croissy-Beaubourg &middot; 2025</figcaption>
          </figure>
        </div>
        <div class="usn-colonne" data-usn-parallaxe="1.9">
          <figure class="usn-affiche">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-4-768x945.jpg" alt="Affiche de Wasquehal 2026" loading="lazy" onerror="this.parentNode.remove()" />
            <figcaption>Wasquehal &middot; 2026</figcaption>
          </figure>
        </div>
        <div class="usn-colonne" data-usn-parallaxe="0.9">
          <figure class="usn-affiche">
            <img src="https://unseulnom.org/wp-content/uploads/2026/04/conference-Un-seul-nom-5-768x950.jpg" alt="Affiche de Perpignan 2026" loading="lazy" onerror="this.parentNode.remove()" />
            <figcaption>Perpignan &middot; 2026</figcaption>
          </figure>
        </div>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- APPEL AUX &#201;GLISES                                              -->
  <!-- ============================================================= -->
  <div class="usn-eglises" id="usn-eglises">
    <div class="usn-wrap">
      <span class="usn-cle" data-usn-monte>&Eacute;glises partenaires</span>
      <h2 data-usn-monte>Une salle ne se remplit pas toute seule</h2>
      <p class="usn-intro" data-usn-monte>
        Chaque &eacute;v&eacute;nement repose sur les &eacute;glises de la ville. Ce sont elles
        qui invitent, qui accompagnent, et qui accueillent ensuite ceux qui
        ont donn&eacute; leur vie &agrave; J&eacute;sus.
      </p>

      <div class="usn-besoins">
        <div><b>Relayer</b>L'annonce aupr&egrave;s de vos membres, sur vos r&eacute;seaux et lors de vos cultes.</div>
        <div><b>Inviter</b>Et accompagner des personnes non croyantes ou malades, jusqu'&agrave; la salle.</div>
        <div><b>Mobiliser</b>Des &eacute;quipes&nbsp;: la rue, les prospectus, la pri&egrave;re pour la ville.</div>
      </div>

      <div class="usn-eglises-actions">
        <a class="usn-btn usn-btn-encre" href="mailto:75unseulnomjesus@gmail.com?subject=Notre%20eglise%20souhaite%20participer">Nous &eacute;crire</a>
        <a class="usn-btn usn-btn-cadre" href="https://www.helloasso.com/associations/eglise-protestante-tziganes/formulaires/3" target="_blank" rel="noopener noreferrer">Soutenir la mission</a>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LETTRE D'INFORMATION                                           -->
  <!-- ============================================================= -->
  <div class="usn-lettre" id="usn-lettre">
    <div class="usn-wrap usn-lettre-grille">
      <div>
        <span class="usn-cle">Restons en lien</span>
        <h2>Ne ratez aucune date</h2>
        <p class="usn-intro">
          D&egrave;s qu'une ville est annonc&eacute;e, vous recevez un message&nbsp;: avant
          l'ouverture des inscriptions, et avant que la salle ne se remplisse.
        </p>
      </div>
      <div>
        <!-- Le formulaire part dans l'iframe cache : le visiteur reste sur
             la page au lieu d'etre redirige vers Brevo. -->
        <iframe name="usn-brevo" title="Inscription" style="display:none;border:0;width:0;height:0"></iframe>

        <form class="usn-form" id="usn-accueil-form" method="POST"
              action="https://f8944495.sibforms.com/serve/MUIFAIs9H4MOwtfawQ-nUiiNQjRmcSNQ51wCfEkSkK4X91IeG5GyZDWqGDV3oD_sUQHzLsQUO6Ky6EqX9G_I6VWvy8H5St2aXpxdhmXp81Jkl34fooTe9ukSalttOBmz1T6EwxTHhs9WLKWs2tBPCT1fhe_uubwAitTQjHmRHfmqBxQBcLifIZRDcFP9W3Vvzs_hPEa9_OySJBsT"
              target="usn-brevo" novalidate>
          <div class="usn-form-duo">
            <input id="usn-nom" name="NOM" type="text" placeholder="Votre nom" aria-label="Nom" required />
            <input id="usn-email" name="EMAIL" type="email" placeholder="vous@email.com" aria-label="Adresse e-mail" required />
          </div>

          <!-- Piege anti-robot de Brevo : ce champ doit rester vide. -->
          <input type="text" name="email_address_check" value="" tabindex="-1"
                 aria-hidden="true" autocomplete="off"
                 style="position:absolute;left:-9999px;width:1px;height:1px;opacity:0" />
          <input type="hidden" name="locale" value="fr" />

          <button type="submit" class="usn-btn usn-btn-bleu">Pr&eacute;venez-moi</button>
          <p class="usn-consent">
            En vous inscrivant, vous acceptez de recevoir des informations de la
            part d'Un Seul Nom. D&eacute;sinscription possible &agrave; tout moment.
          </p>
        </form>

        <div class="usn-msg" id="usn-form-msg" role="status" aria-live="polite"></div>
      </div>
    </div>
  </div>

  <div class="usn-defile usn-defile-encre" aria-hidden="true">
    <div class="usn-defile-piste">
      <div class="usn-defile-suite">
        <b>Avignon</b> <i>&#10022;</i> <b>Madagascar</b> <i>&#10022;</i>
        <b>Marseille</b> <i>&#10022;</i> <b>Bordeaux</b> <i>&#10022;</i>
      </div>
      <div class="usn-defile-suite">
        <b>Avignon</b> <i>&#10022;</i> <b>Madagascar</b> <i>&#10022;</i>
        <b>Marseille</b> <i>&#10022;</i> <b>Bordeaux</b> <i>&#10022;</i>
      </div>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- PIED DE PAGE                                                   -->
  <!-- ============================================================= -->
  <footer class="usn-footer">
    <div class="usn-wrap">
      <div class="usn-footer-grille">
        <div class="usn-logo-mark usn-logo-blanc usn-footer-mark" role="img" aria-label="Un Seul Nom"></div>
        <div class="usn-footer-liens">
          <a href="https://unseulnom.org/evenement/">L'&eacute;v&eacute;nement</a>
          <a href="#usn-vision">La vision</a>
          <a href="#usn-eglises">&Eacute;glises</a>
          <a href="#usn-lettre">Newsletter</a>
          <a href="https://www.helloasso.com/associations/eglise-protestante-tziganes/formulaires/3" target="_blank" rel="noopener noreferrer">Faire un don</a>
        </div>
      </div>
      <div class="usn-footer-bas">&copy; 2026 Un Seul Nom. Tous droits r&eacute;serv&eacute;s.</div>
    </div>
  </footer>

</section>

<script>
/* Tout le code attend que la page soit construite. Sans cela, si le script
   est insere avant le contenu (ce que font certains plugins WordPress), il
   ne trouve aucun element et s'arrete sans rien faire. */
(function () {
  function usnInit() {

  /* ---- Le menu du haut ----
     Le bouton a deux traits ouvre le panneau ; les deux traits se
     croisent, la page se bloque derriere, et Echap referme. */
  (function () {
    var bouton = document.getElementById('usn-bouton-menu');
    var panneau = document.getElementById('usn-menu');
    if (!bouton || !panneau) return;

    function basculer(ouvrir) {
      bouton.setAttribute('aria-expanded', ouvrir ? 'true' : 'false');
      bouton.setAttribute('aria-label', ouvrir ? 'Fermer le menu' : 'Ouvrir le menu');
      if (ouvrir) {
        panneau.hidden = false;
        // Un temps de rendu avant la classe, sinon la transition est sautee.
        requestAnimationFrame(function () { panneau.classList.add('est-ouvert'); });
      } else {
        panneau.classList.remove('est-ouvert');
        setTimeout(function () {
          if (bouton.getAttribute('aria-expanded') === 'false') panneau.hidden = true;
        }, 340);
      }
      document.documentElement.style.overflow = ouvrir ? 'hidden' : '';
    }

    bouton.addEventListener('click', function () {
      basculer(bouton.getAttribute('aria-expanded') !== 'true');
    });
    // Un lien clique referme le panneau : sinon l'ancre saute derriere lui.
    panneau.addEventListener('click', function (e) {
      if (e.target.closest('a')) basculer(false);
    });
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape' && bouton.getAttribute('aria-expanded') === 'true') basculer(false);
    });
  })();

  /* ---- La barre se pose sur un fond quand on quitte le heros ---- */
  (function () {
    var nav = document.getElementById('usn-nav');
    if (!nav) return;
    function verifier() {
      nav.classList.toggle('est-posee', (window.pageYOffset || 0) > 40);
    }
    window.addEventListener('scroll', verifier, { passive: true });
    verifier();
  })();

  /* ---- La photo change a chaque passage de la lame ----
     Les balayages sont entierement en CSS ; ce script se contente de
     faire tourner les photos, une par passage. */
  (function () {
    var lames = document.querySelectorAll('#usn-accueil .usn-balai');
    for (var n = 0; n < lames.length; n++) {
      (function (lame) {
        var vues = lame.querySelectorAll('img');
        if (vues.length < 2) return;
        var i = 0;
        lame.addEventListener('animationiteration', function () {
          vues[i].classList.remove('est-vue');
          i = (i + 1) % vues.length;
          vues[i].classList.add('est-vue');
        });
      })(lames[n]);
    }
  })();

  /* ---- La fenetre qui suit le doigt ou la souris ----
     Une fois la lame passee, c'est au visiteur de decouvrir le reste de
     la photo. On ecoute "pointermove" : le meme code sert donc a la
     souris, au doigt et au stylet. */
  (function () {
    var bloc   = document.getElementById('usn-bloc1');
    var hublot = document.getElementById('usn-hublot');
    var anneau = document.getElementById('usn-anneau');
    if (!bloc || !hublot) return;

    var vues = hublot.querySelectorAll('img');
    var iVue = 0, dernierEchange = 0;
    var cible = { x: 50, y: 46 }, courant = { x: 50, y: 46 };
    var actif = false, anime = false;

    function rayon() {
      // 17vmin, la meme valeur que le CSS, mais en pixels : l'anneau doit
      // tomber exactement sur le bord du masque.
      return Math.min(window.innerWidth, window.innerHeight) * 0.17;
    }

    function allumer(oui) {
      if (actif === oui) return;
      actif = oui;
      hublot.classList.toggle('est-actif', oui);
      if (anneau) anneau.classList.toggle('est-actif', oui);
    }

    function boucle(t) {
      courant.x += (cible.x - courant.x) * 0.14;
      courant.y += (cible.y - courant.y) * 0.14;
      bloc.style.setProperty('--mx', courant.x.toFixed(2) + '%');
      bloc.style.setProperty('--my', courant.y.toFixed(2) + '%');

      if (anneau) {
        var r = rayon();
        anneau.style.setProperty('--usn-rayon', r.toFixed(1) + 'px');
        anneau.style.transform = 'translate3d('
          + (courant.x / 100 * bloc.clientWidth - r).toFixed(1) + 'px,'
          + (courant.y / 100 * bloc.clientHeight - r).toFixed(1) + 'px,0)';
      }

      // On change de photo au bout d'un moment, pas a chaque mouvement :
      // sinon les images clignotent des qu'on bouge la souris.
      if (vues.length > 1 && actif && t - dernierEchange > 1800) {
        dernierEchange = t;
        vues[iVue].classList.remove('est-vue');
        iVue = (iVue + 1) % vues.length;
        vues[iVue].classList.add('est-vue');
      }

      var bouge = Math.abs(cible.x - courant.x) > 0.05 || Math.abs(cible.y - courant.y) > 0.05;
      if (bouge || actif) { requestAnimationFrame(boucle); } else { anime = false; }
    }

    function lancer() { if (!anime) { anime = true; requestAnimationFrame(boucle); } }

    function suivre(e) {
      var r = bloc.getBoundingClientRect();
      if (!r.width) return;
      cible.x = ((e.clientX - r.left) / r.width) * 100;
      cible.y = ((e.clientY - r.top) / r.height) * 100;
      allumer(true);
      lancer();
    }

    // Passif : le doigt continue de faire defiler la page normalement.
    bloc.addEventListener('pointermove', suivre, { passive: true });
    bloc.addEventListener('pointerdown', suivre, { passive: true });
    bloc.addEventListener('pointerleave', function () { allumer(false); });
    // Au telephone, le doigt quitte l'ecran : on laisse la fenetre
    // ouverte un instant plutot que de l'effacer d'un coup.
    bloc.addEventListener('pointerup', function () {
      setTimeout(function () { allumer(false); }, 1400);
    });
  })();

  /* ---- Son de la video ----
     Aucun navigateur ne lance seul une video qui parle : elle demarre
     donc muette, et ce bouton rend la main au visiteur. S'il n'y a pas
     de piste audio dans le fichier, le bouton se retire de lui-meme
     plutot que de promettre un son qui n'existe pas. */
  (function () {
    var video  = document.querySelector('#usn-accueil .usn-hero-video');
    var bouton = document.getElementById('usn-son');
    if (!video || !bouton) return;

    function aDuSon() {
      // Chaque navigateur renseigne un indicateur different ; aucun n'est
      // disponible partout, d'ou les trois essais successifs.
      if (typeof video.mozHasAudio === 'boolean') return video.mozHasAudio;
      if (video.webkitAudioDecodedByteCount !== undefined) {
        return video.webkitAudioDecodedByteCount > 0;
      }
      if (video.audioTracks) return video.audioTracks.length > 0;
      return true;   // dans le doute, on laisse le bouton
    }

    function verifier() {
      if (!aDuSon()) bouton.remove();
    }
    if (video.readyState >= 2) { verifier(); }
    video.addEventListener('loadeddata', verifier);

    function libelle() {
      var texte = bouton.querySelector('.usn-son-texte');
      var icone = bouton.querySelector('.usn-son-icone');
      if (texte) texte.textContent = video.muted ? 'Activer le son' : 'Couper le son';
      if (icone) icone.innerHTML = video.muted ? '&#9834;' : '&#9835;';
      bouton.setAttribute('aria-pressed', video.muted ? 'false' : 'true');
    }
    libelle();

    bouton.addEventListener('click', function () {
      video.muted = !video.muted;
      if (!video.muted) { video.volume = 0.8; video.play(); }
      libelle();
    });
  })();


  /* =================================================================
     MOTEUR D'ANIMATION AU SCROLL
     Reproduit le comportement de GSAP ScrollTrigger en mode " scrub " :
     - les elements partent d'une demi-hauteur d'ecran plus bas (50svh),
     - ils remontent AU RYTHME DU SCROLL (et redescendent si on remonte),
     - avec un chevauchement entre les elements d'un meme groupe,
     - le tout lisse (inertie) pour un rendu fluide.
     Sans JS ou avec " prefers-reduced-motion ", tout reste visible.
     ================================================================= */
  (function () {
    var root = document.getElementById('usn-accueil');
    if (!root) return;
    var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    function clamp01(v) { return v < 0 ? 0 : v > 1 ? 1 : v; }
    function easeOut(t) { return 1 - Math.pow(1 - t, 3); }
    function vh() { return window.innerHeight || 800; }

    /* ---------- 1. Decoupe la punchline en mots ---------- */
    var punch = document.getElementById('usn-punch');
    var words = [];
    if (punch) {
      var lines = punch.querySelectorAll('[data-usn-reveal]');
      for (var l = 0; l < lines.length; l++) {
        var parts = (lines[l].textContent || '').trim().split(/\s+/);
        lines[l].textContent = '';
        for (var k = 0; k < parts.length; k++) {
          var sp = document.createElement('span');
          sp.className = 'usn-word';
          sp.textContent = parts[k];
          lines[l].appendChild(sp);
          if (k < parts.length - 1) lines[l].appendChild(document.createTextNode(' '));
          words.push(sp);
        }
      }
      for (var w = 0; w < words.length; w++) {
        var a = Math.sin(w * 12.9898) * 43758.5453; a -= Math.floor(a);
        var b = Math.sin(w * 78.233) * 12543.123;  b -= Math.floor(b);
        words[w].__dx = (a - 0.5) * 2 * Math.min(window.innerWidth * 0.45, 520);
        words[w].__dy = (b - 0.5) * 2 * 260;
        words[w].__rot = (a - 0.5) * 55;
      }
    }

    if (reduce) return; // mouvement desactive : tout reste en place

    /* ---------- 2. Declare les groupes animes ---------- */
    // rise : monte depuis 50svh ; slide : arrive de la gauche ; pop : grossit
    var groups = [];
    function group(sel, opts) {
      var els = root.querySelectorAll(sel);
      if (els.length) groups.push({ els: els, o: opts });
    }
    group('[data-usn-monte]',   { type: 'rise',  dist: 0.28, stagger: 0.10, span: 0.52 });
    group('.usn-vue',           { type: 'rise',  dist: 0.36, stagger: 0.07, span: 0.54 });
    group('.usn-chiffre',       { type: 'rise',  dist: 0.22, stagger: 0.10, span: 0.46 });
    group('[data-usn-gauche]',  { type: 'slide',  dist: 0.55, stagger: 0, span: 0.52 });
    group('[data-usn-droite]',  { type: 'slideR', dist: 0.55, stagger: 0, span: 0.52 });

    var all = [];
    for (var g = 0; g < groups.length; g++) {
      for (var i = 0; i < groups[g].els.length; i++) {
        var el = groups[g].els[i];
        all.push({
          el: el, i: i, o: groups[g].o, p: 0, top: 0,
          rot: parseFloat(el.getAttribute('data-rot') || '0')
        });
      }
    }

    function scrollY() { return window.pageYOffset || document.documentElement.scrollTop || 0; }

    /* Position NATURELLE de chaque element (transform neutralise), mise en
       cache : sinon le decalage applique fausse la mesure et l'element
       n'entre jamais dans la zone de declenchement. */
    function measureAll() {
      for (var i = 0; i < all.length; i++) {
        var it = all[i], prev = it.el.style.transform;
        it.el.style.transform = 'none';
        it.top = it.el.getBoundingClientRect().top + scrollY();
        it.el.style.transform = prev;
      }
    }
    measureAll();

    /* ---------- Portraits : couleur au passage au centre ----------
       Chaque photo se colore a mesure qu'elle approche du centre de
       l'ecran, puis retourne au noir et blanc en s'en eloignant. */
    var photos = [];
    (function () {
      var imgs = root.querySelectorAll('.usn-vue img, .usn-visage img');
      for (var i = 0; i < imgs.length; i++) photos.push({ el: imgs[i], p: 0 });
    })();

    function cibleCouleur(ph) {
      var r = ph.el.getBoundingClientRect();
      if (!r.height) return 0;
      var h = vh();
      var ecart = Math.abs((r.top + r.height / 2) - h / 2);
      // Pleine couleur au centre, noir et blanc au-dela de 42% de l'ecran.
      return clamp01(1 - ecart / (h * 0.42));
    }

    function peindre(ph) {
      var e = easeOut(ph.p);
      ph.el.style.filter = 'grayscale(' + (1 - e).toFixed(3) + ') contrast(' + (1 + 0.06 * (1 - e)).toFixed(3) + ')';
      ph.el.style.transform = 'scale(' + (1 + 0.035 * e).toFixed(4) + ')';
    }
    for (var ip = 0; ip < photos.length; ip++) peindre(photos[ip]);

    // etat initial immediat (evite tout " flash " de contenu deja en place)
    for (var s = 0; s < all.length; s++) apply(all[s], 0);

    /* ---------- 3. Calcul de l'avancement lie au scroll ---------- */
    function target(item) {
      var h = vh();
      var top = item.top - scrollY();
      // 0 quand le haut de l'element entre par le bas, 1 quand il a monte
      var raw = (h * 0.92 - top) / (h * item.o.span);
      var st = item.i * item.o.stagger;
      return clamp01((clamp01(raw) - st) / Math.max(0.2, 1 - st));
    }

    function apply(item, p) {
      var e = easeOut(p), inv = 1 - e, o = item.o, st = item.el.style;
      var rot = item.rot ? ' rotate(' + item.rot + 'deg)' : '';
      if (o.type === 'rise') {
        st.transform = 'translate3d(0,' + (inv * o.dist * vh()).toFixed(1) + 'px,0)' + rot;
      } else if (o.type === 'slide') {
        st.transform = 'translate3d(' + (-inv * o.dist * Math.min(window.innerWidth, 900)).toFixed(1) + 'px,0,0)' + rot;
      } else if (o.type === 'slideR') {
        st.transform = 'translate3d(' + (inv * o.dist * Math.min(window.innerWidth, 900)).toFixed(1) + 'px,0,0)' + rot;
      } else { // pop
        st.transform = 'scale(' + (0.3 + 0.7 * e).toFixed(3) + ')' + rot;
      }
      st.opacity = e.toFixed(3);
    }

    /* ---------- 4. Punchline : les mots se rassemblent ---------- */
    var punchP = 0;
    // Ancre sur la premiere ligne de texte (et non sur la section, bien plus
    // haute) : les mots se rassemblent des que le texte entre a l'ecran.
    var punchAnchor = punch && punch.querySelector('[data-usn-reveal]');
    function punchTarget() {
      if (!punchAnchor) return 0;
      var r = punchAnchor.getBoundingClientRect(), h = vh();
      return clamp01((h * 1.0 - r.top) / (h * 0.5));
    }
    function punchApply(p) {
      var n = words.length;
      for (var i = 0; i < n; i++) {
        var t = clamp01((p - (i / n) * 0.45) / 0.55);
        var e = easeOut(t), inv = 1 - e, ws = words[i];
        ws.style.transform = 'translate3d(' + (ws.__dx * inv).toFixed(1) + 'px,' + (ws.__dy * inv).toFixed(1) + 'px,0) rotate(' + (ws.__rot * inv).toFixed(1) + 'deg)';
        ws.style.opacity = e.toFixed(3);
        ws.style.filter = inv > 0.02 ? 'blur(' + (inv * 9).toFixed(1) + 'px)' : 'none';
      }
      var sub = punch && punch.querySelector('[data-usn-sub]');
      if (sub) {
        var sp = clamp01((p - 0.6) / 0.4);
        sub.style.opacity = sp.toFixed(3);
        sub.style.transform = 'translate3d(0,' + ((1 - sp) * 40).toFixed(1) + 'px,0)';
      }
    }
    punchApply(0);

    /* ---------- 5. La scene d'ouverture ----------
       Le bloc fait deux hauteurs d'ecran et son contenu reste colle en
       haut : pendant qu'on le traverse, le logo s'efface et l'image
       derriere prend sa place. */
    var scene  = root.querySelector('.usn-scene');
    var couche1 = root.querySelector('.usn-couche-logo');
    var couche2 = root.querySelector('.usn-couche-image');

    function sceneAppliquer() {
      if (!scene || !couche1 || !couche2) return;
      var r = scene.getBoundingClientRect();
      var course = Math.max(1, r.height - vh());
      var p = clamp01(-r.top / course);

      // Le logo s'efface sur la premiere moitie, l'image n'arrive qu'apres :
      // sans ce decalage, les deux "Un Seul Nom" se superposent au milieu du
      // fondu et plus rien n'est lisible.
      var sortie = clamp01(p / 0.42);
      couche1.style.opacity = (1 - sortie).toFixed(3);
      couche1.style.transform = 'scale(' + (1 - 0.26 * easeOut(sortie)).toFixed(4) + ')';

      var entree = clamp01((p - 0.40) / 0.42);
      couche2.style.opacity = entree.toFixed(3);
      couche2.style.transform = 'scale(' + (1.10 - 0.10 * easeOut(entree)).toFixed(4) + ')';

      // Une couche effacee ne doit plus rien capter : sans cela, le bouton
      // "Reserve ta place" reste cliquable alors qu'on ne le voit plus.
      couche1.style.pointerEvents = sortie > 0.6 ? 'none' : 'auto';
      couche2.style.pointerEvents = entree > 0.6 ? 'auto' : 'none';
    }
    sceneAppliquer();

    /* ---------- 6. Parallaxes ----------
       Chaque element marque monte a sa propre vitesse pendant qu'il
       traverse l'ecran : c'est ce qui donne de la profondeur a la
       colonne d'affiches. */
    var paras = root.querySelectorAll('[data-usn-parallaxe]');
    function parallaxes() {
      for (var i = 0; i < paras.length; i++) {
        var el = paras[i], r = el.getBoundingClientRect(), h = vh();
        var p = clamp01((h - r.top) / (h + r.height));
        var v = parseFloat(el.getAttribute('data-usn-parallaxe')) || 1;
        el.style.transform = 'translate3d(0,' + ((0.5 - p) * 150 * v).toFixed(1) + 'px,0)';
      }
    }
    parallaxes();

    /* ---------- 7. Les deux visages qui se rapprochent ---------- */
    var visages = root.querySelector('.usn-visages');
    function visagesAppliquer() {
      if (!visages) return;
      var r = visages.getBoundingClientRect(), h = vh();
      // 0 quand le bloc entre par le bas, 1 quand il est au centre
      var p = clamp01((h - r.top) / (h * 0.85 + r.height * 0.4));
      var e = easeOut(p), ecart = (1 - e) * Math.min(window.innerWidth * 0.42, 460);
      var g = visages.querySelector('[data-usn-visage="gauche"]');
      var d = visages.querySelector('[data-usn-visage="droite"]');
      if (g) g.style.transform = 'translate3d(' + (-ecart).toFixed(1) + 'px,0,0)';
      if (d) d.style.transform = 'translate3d(' + ecart.toFixed(1) + 'px,0,0)';
    }
    visagesAppliquer();

    var hero = null, heroInner = null;

    /* ---------- 6. Boucle de rendu (lissage type ScrollSmoother) ---------- */
    var running = false;
    var dernierY = -1;
    function frame() {
      var moving = false;

      // Ces trois-la suivent le scroll directement : tant qu'il bouge,
      // la boucle continue.
      var y = scrollY();
      if (y !== dernierY) { dernierY = y; moving = true; }
      sceneAppliquer();
      parallaxes();
      visagesAppliquer();

      for (var i = 0; i < all.length; i++) {
        var it = all[i], t = target(it);
        it.p += (t - it.p) * 0.12;             // lissage
        if (Math.abs(t - it.p) > 0.001) moving = true; else it.p = t;
        apply(it, it.p);
      }

      if (punch) {
        var tp = punchTarget();
        punchP += (tp - punchP) * 0.12;
        if (Math.abs(tp - punchP) > 0.001) moving = true; else punchP = tp;
        punchApply(punchP);
      }

      for (var k = 0; k < photos.length; k++) {
        var ph = photos[k], tc = cibleCouleur(ph);
        ph.p += (tc - ph.p) * 0.14;
        if (Math.abs(tc - ph.p) > 0.004) moving = true; else ph.p = tc;
        peindre(ph);
      }

      if (hero) {
        var hr = hero.getBoundingClientRect();
        var prog = clamp01(-hr.top / Math.max(1, hr.height));
        if (heroInner) {
          heroInner.style.transform = 'translate3d(0,' + (prog * 130).toFixed(1) + 'px,0)';
          heroInner.style.opacity = (1 - prog * 0.85).toFixed(3);
        }
      }

      if (moving) { requestAnimationFrame(frame); } else { running = false; }
    }
    function kick() { if (!running) { running = true; requestAnimationFrame(frame); } }

    window.addEventListener('scroll', kick, { passive: true });
    window.addEventListener('resize', function () { measureAll(); kick(); });
    window.addEventListener('load', function () { measureAll(); kick(); });
    kick();
  })();


  /* ---- Lettre d'information ---- */
  (function () {
    var form = document.getElementById('usn-accueil-form');
    if (!form) return;
    var msg = document.getElementById('usn-form-msg');

    form.addEventListener('submit', function (e) {
      var nom   = form.querySelector('#usn-nom');
      var email = form.querySelector('#usn-email');
      if (!nom.value.trim() || !/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email.value)) {
        e.preventDefault();
        (nom.value.trim() ? email : nom).focus();
        return;
      }
      if (msg) {
        msg.textContent = 'Merci ! Vous serez pr\u00e9venu d\u00e8s qu\'une nouvelle date est annonc\u00e9e.';
        msg.style.display = 'block';
      }
      setTimeout(function () { form.reset(); }, 100);
    });
  })();

  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', usnInit);
  } else {
    usnInit();
  }
})();
</script>
